# Project instructions

Maintain `PROMPTS.md` as a chronological log of user prompts for this project.
Append each new user prompt verbatim under its date, with a sequential entry
number. Do not duplicate existing entries. Exclude automatically supplied
environment/plugin context and assistant responses. Redact secrets if present
and mark the redaction explicitly.
